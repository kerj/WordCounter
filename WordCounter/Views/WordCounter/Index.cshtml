@using WordCounter.Models;

<h1>WordCounter</h1>
  @if (@Model.Count == 0)
  {
    <h3>No Searches have been added yet!</h3>
  }
  @foreach (var wordCounter in Model)
  {
    <h3><a href='/wordCounter/@wordCounter.Id'>@wordCounter.SearchTerm</a></h3>
  }

  <p><a href='/'>Back Home</a></p>
